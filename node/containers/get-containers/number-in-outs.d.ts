import type { Container } from '../container.js';
/**
 * Simply gives each `InOut` within all containers a unique `idx`
 *
 * @param containers
 */
declare function numberInOuts(containers: Container[]): void;
export { numberInOuts };
